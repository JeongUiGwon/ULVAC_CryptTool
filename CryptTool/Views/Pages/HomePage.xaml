<Page x:Class="CryptTool.Views.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CryptTool.Views.Pages" 
      xmlns:vm="clr-namespace:CryptTool.ViewModels.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage" Background="White">

    <Page.DataContext>
        <vm:HomeViewModel/>
    </Page.DataContext>

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock FontSize="24"
                   FontWeight="Bold"
                   Text="ULVAC Crypt Tool"/>

        <!-- Version -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <TextBlock FontSize="16"
                       Text="{Binding Version}"/>
        </StackPanel>

        <!-- Release Notes -->
        <GroupBox Grid.Row="4" Header="Release Notes">
            <TextBox Text="{Binding ReleaseNoteText, Mode=OneWay}"
                     Margin="5"
                     BorderThickness="0"
                     Background="Transparent"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"/>
        </GroupBox>
    </Grid>
</Page>
