<Page x:Class="CryptTool.Views.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CryptTool.Views.Pages" 
      xmlns:vm="clr-namespace:CryptTool.ViewModels.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage" Background="White">

    <Page.DataContext>
        <vm:HomeViewModel/>
    </Page.DataContext>

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock FontSize="24"
                   FontWeight="Bold"
                   Text="ULVAC Crypt Tool"/>

        <!-- Version -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <TextBlock FontSize="16"
                       Margin="0,6"
                       Text="{Binding Version}"/>
        </StackPanel>

        <!-- Release Note Card -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="10"
                BorderBrush="#E1E4E8"
                BorderThickness="1">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontSize="18"
                               FontWeight="Bold"
                               Foreground="#333333"
                               Text="Release Notes"/>
                </StackPanel>

                <!-- Divider -->
                <Border Grid.Row="1"
                        Height="1"
                        Background="#E5E7EB"
                        Margin="0,6,0,6"/>

                <!-- Content -->
                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">

                    <TextBlock Text="{Binding ReleaseNoteText, Mode=OneWay}"
                               FontSize="14"
                               Foreground="#444444"
                               TextWrapping="Wrap"
                               LineHeight="22"/>
                </ScrollViewer>

            </Grid>
        </Border>
    </Grid>
</Page>
